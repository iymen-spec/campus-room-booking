spring.application.name=campus-room-booking

# Temporarily disable database auto-configuration until PostgreSQL is configured.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
